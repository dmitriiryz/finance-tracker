function renderCharts(){let ex=monthTx().filter(t=>t.type==='expense'),tot={};ex.forEach(t=>tot[t.catId]=(tot[t.catId]||0)+ars(t));let ids=Object.keys(tot),wrap=document.getElementById('donut-wrap');if(donutChart){donutChart.destroy();donutChart=null}if(!ids.length)wrap.innerHTML='<div class="empty">Sin gastos en el mes seleccionado</div>';else{wrap.innerHTML='<div class="chart-wrap" style="height:240px"><canvas id="c-donut"></canvas></div>';donutChart=new Chart(document.getElementById('c-donut'),{type:'doughnut',data:{labels:ids.map(id=>label(cat(id))),datasets:[{data:ids.map(id=>tot[id]),backgroundColor:['#6c63ff','#2ecc8f','#ff5c5c','#ffaa00','#3ab5e5','#ff8c69','#a78bfa','#888899'],borderWidth:0}]},options:{responsive:true,maintainAspectRatio:false,cutout:'65%',plugins:{legend:{position:'bottom',labels:{color:'#888899',font:{size:11,family:'Manrope'},boxWidth:10}},tooltip:{callbacks:{label:i=>i.label+': '+fmt(i.raw)}}}}})}let d=new Date(),bm=[];for(let i=5;i>=0;i--){let x=new Date(d.getFullYear(),d.getMonth()-i,1);bm.push({key:x.getFullYear()+'_'+String(x.getMonth()+1).padStart(2,'0'),label:MS[x.getMonth()]})}let bi=bm.map(m=>transactions.filter(t=>t.type==='income'&&monthKey(t)===m.key).reduce((s,t)=>s+ars(t),0)),be=bm.map(m=>transactions.filter(t=>t.type==='expense'&&monthKey(t)===m.key).reduce((s,t)=>s+ars(t),0));if(barChart)barChart.destroy();barChart=new Chart(document.getElementById('c-bar'),{type:'bar',data:{labels:bm.map(m=>m.label),datasets:[{label:'Ingresos',data:bi,backgroundColor:'rgba(46,204,143,.6)',borderRadius:5},{label:'Gastos',data:be,backgroundColor:'rgba(255,92,92,.6)',borderRadius:5}]},options:{responsive:true,maintainAspectRatio:false,plugins:{legend:{labels:{color:'#888899',font:{size:11,family:'Manrope'},boxWidth:10}}},scales:{x:{ticks:{color:'#888899'},grid:{display:false}},y:{ticks:{color:'#888899',callback:v=>v>=1000?Math.round(v/1000)+'k':v},grid:{color:'rgba(255,255,255,.05)'}}}}})}
